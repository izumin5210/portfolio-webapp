name: Setup
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v2
      with:
        node-version-file: '.node-version'
        cache: 'yarn'
    - run: yarn --immutable
    - run: yarn bootstrap
